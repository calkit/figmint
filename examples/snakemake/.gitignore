# `provenance.toml` is NOT here, and neither are the figures. The record is the
# evidence this project is about, and an artifact nobody committed is one
# nobody can check.
#
# Snakemake's own state is a different matter: it is a cache, local and
# disposable, which is the distinction the README draws.
/.snakemake/
/.venv/
