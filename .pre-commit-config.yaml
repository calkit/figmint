repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: "v4.4.0"
    hooks:
      - id: check-case-conflict
      - id: check-merge-conflict
      # The examples' figures are signed artifacts whose hashes `fromwhere` has
      # recorded, so a hook that rewrites their bytes makes the record wrong.
      # Every example, not just the first one: the same pattern as
      # `.prettierignore`, which has always covered all of them.
      - id: end-of-file-fixer
        exclude: ^examples/[^/]+/(figures|results)/
      - id: trailing-whitespace
        exclude: ^examples/[^/]+/(figures|results)/
      - id: check-yaml
        args: ["--unsafe"]
      - id: check-json

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: "v0.6.3"
    hooks:
      - id: ruff
        args: [--exit-non-zero-on-fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: "v3.0.3"
    hooks:
      - id: prettier
