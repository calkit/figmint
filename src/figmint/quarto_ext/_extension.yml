title: figmint
author: figmint
version: 1.0.0
quarto-required: ">=1.4.0"
contributes:
  filters:
    # Referenced from the document as `{at: pre-ast, path: figmint}`. The
    # position is not optional: callouts, figure numbering and cross-references
    # are built by Quarto's own filters, so a panel emitted after them is a
    # plain div and an unnumbered picture.
    - figmint.lua
